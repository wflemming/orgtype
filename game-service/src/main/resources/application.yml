spring:
  application:
    name: game-service

server:
  port: 8082

org-service:
  url: ${ORG_SERVICE_URL:http://localhost:8081}

management:
  endpoints:
    web:
      base-path: /management
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
